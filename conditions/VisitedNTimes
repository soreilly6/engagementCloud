/*
Output Config: 
Visitor has visited [[N]] times 
*/


(function () {
  let count = `[[N | number]]`

  if (guest.sessions && guest.sessions.length >= count) 
    return true;
  
  return false;
})();
